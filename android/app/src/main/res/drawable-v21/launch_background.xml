<?xml version="1.0" encoding="utf-8"?>
<!-- Launch splash background.

     Was ?android:colorBackground, which follows the SYSTEM theme: on a phone
     in dark mode the splash went black while the app itself is light-only.
     Pinned to the app's own colour so the two agree. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />

    <!-- You can insert your own image assets here -->
    <!-- <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/launch_image" />
    </item> -->
</layer-list>
